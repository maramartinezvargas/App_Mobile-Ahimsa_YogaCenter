<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_login.xml

    Pantalla de inicio de sesión.
    Contiene:
    - Logo de la aplicación.
    - Campos de entrada: correo electrónico y contraseña.
    - Botón Login para validar credenciales.
    - Texto para "Olvidé mi contraseña".
    - Texto para redirigir al registro de nueva cuenta.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/login_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="100dp"
        android:paddingLeft="24dp"
        android:paddingRight="24dp"
        android:gravity="center">

        <!-- Logo de la app -->
        <ImageView
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_marginBottom="14dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/ahimsa_logo" />

        <!-- Título -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Iniciar sesión"
            android:textSize="20dp"
            android:textColor="#ffffff"
            android:layout_marginBottom="24dp" />

        <!-- Entrada de correo -->
        <EditText
            android:id="@+id/inputEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:background="#30FFFFFF"
            android:clipToOutline="true"
            android:hint="Correo electrónico"
            android:textSize="16dp"
            android:inputType="textEmailAddress"
            android:outlineProvider="background"
            android:padding="12dp"
            android:textColor="#ffffff"
            android:textColorHint="#ffffff" />

        <!-- Entrada de contraseña -->
        <EditText
            android:id="@+id/inputPassword"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Contraseña"
            android:inputType="textPassword"
            android:layout_marginBottom="24dp"
            android:background="#30FFFFFF"
            android:padding="12dp"
            android:textSize="16dp"
            android:textColor="#ffffff"
            android:textColorHint="#ffffff"
            android:clipToOutline="true"
            android:outlineProvider="background"/>

        <!-- Botón login -->
        <Button
            android:id="@+id/btnLogin"
            android:layout_width="189dp"
            android:layout_height="wrap_content"
            android:backgroundTint="#a98ea8"
            android:text="Login"
            android:textSize="16dp"
            android:textStyle="bold"
            android:textColor="@android:color/white"
            android:textColorHighlight="#ffffff" />

        <!-- Enlace olvidé contraseña -->
        <TextView
            android:id="@+id/textForgotPassword"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¿Has olvidado tu contraseña?"
            android:textColorHighlight="#ffffff"
            android:textColor="#ffffff"
            android:layout_marginTop="16dp" />

        <!-- Enlace registro nuevo usuario -->
        <TextView
            android:id="@+id/textRegister"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="¿No tienes cuenta? Regístrate aquí"
            android:textColor="#ffffff"
            android:textStyle="bold"
            android:layout_marginTop="12dp" />

    </LinearLayout>
</ScrollView>
